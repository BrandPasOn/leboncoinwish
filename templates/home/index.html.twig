{% extends 'base.html.twig' %}

{% block title %}Le Bon Kouin - Home Page{% endblock %}

{% block body %}

<div class="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">

    <h1 class="text-2xl font-bold text-center">Bienvenue sur Le Bon Kouin !</h1>
    <section class="flex gap-8">
        {% if posts %}
            {% for post in posts %}
                <a class="p-2 border-2 w-3/6" href="/post/{{ post.id }}">
                    <article>
                        <h2 class="text-xl font-bold tracking-tight">{{ post.title }}</h2>
                        <p>{{ post.price }} €</p>
                    </article>
                </a>
            {% endfor %}
        {% endif %}
    </section>


</div>
{% endblock %}
